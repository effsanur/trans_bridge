import 'package:flutter/widgets.dart';

class PastView extends StatelessWidget {
  const PastView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Geçmiş'));
  }
}
